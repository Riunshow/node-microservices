{
  "name": "@turbine/collect-svc",
  "version": "0.0.1",
  "license": "MIT",
  "main": "app.ts",
  "scripts": {
    "test": "jest --config .jest.config.js --no-cache --detectOpenHandles",
    "prettier": "prettier --write '**/*.{js,ts}'",
    "dev": "nodemon --config nodemon.json",
    "deploy": "prisma migrate deploy --schema ./node_modules/@turbine/schema/generated/schema.prisma",
    "start": "node -r dotenv/config ./dist/app.js",
    "build": "zx scripts/build.mjs"
  },
  "engines": {
    "node": ">= 12.0"
  },
  "prettier": "@geist-ui/prettier-config",
  "dependencies": {
    "@turbine/proto-files": "^1.0.0",
    "@turbine/schema": "^1.0.0",
    "dotenv": "^10.0.0",
    "koa": "^2.13.1",
    "koa-bodyparser": "^4.3.0",
    "koa-logger": "^3.2.1",
    "koa-multer": "^1.0.2",
    "koa-router": "^10.0.0",
    "node-zookeeper-client": "^1.1.0",
    "reflect-metadata": "^0.1.13",
    "routing-controllers": "^0.9.0",
    "typedi": "^0.10.0",
    "prisma": "^2.27.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
